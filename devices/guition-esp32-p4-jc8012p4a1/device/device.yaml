# =============================================================================
# HARDWARE CONFIGURATION - Guition ESP32-P4-JC8012P4A1
# =============================================================================
# Hardware settings for the ESP32-P4 10.1" 1280x800 landscape touchscreen.
# Uses MIPI DSI display, GSL3680 touch controller, and ESP32-C6 coprocessor
# for WiFi/BT connectivity.
#
# WARNING: These settings are specific to the Guition ESP32-P4-JC8012P4A1.
#          Using this config with a different device may cause issues.
# =============================================================================


esp32:
  variant: ESP32P4
  flash_size: 16MB
  engineering_sample: true
  framework:
    type: esp-idf
    version: recommended
    sdkconfig_options:
      CONFIG_HTTPD_MAX_URI_LEN: "128"
      CONFIG_LV_DRAW_SW_SUPPORT_RGB565: "y"
    advanced:
      enable_idf_experimental_features: true


esphome:
  name: ${name}
  friendly_name: ${friendly_name}
  on_boot:
    priority: -10
    then:
      - delay: 2s
      - script.execute: apply_timezone
      - light.turn_on:
          id: backlight
          brightness: 100%
          transition_length: 500ms
      - script.execute: backlight_wake_timeout
      - wait_until:
          condition:
            wifi.connected:
          timeout: 30s
      - lambda: |-
          lv_obj_clear_flag(id(loading_screen), LV_OBJ_FLAG_HIDDEN);
          lv_obj_move_foreground(id(loading_screen));
      - delay: 1s
      - lambda: |-
          lv_obj_clear_flag(id(loading_screen), LV_OBJ_FLAG_HIDDEN);
          lv_obj_move_foreground(id(loading_screen));
      - delay: 4s
      - script.execute: load_favorites_from_json
      - script.execute: load_exclude_players
      - script.execute: parse_unavailable_speakers
      - lambda: 'id(open_favorites_on_boot) = false;'
      - if:
          condition:
            lambda: |-
              std::string s = id(media_player_state_sensor).state;
              return id(media_player_state_sensor).has_state() &&
                     (s == "playing" || s == "paused");
          then:
            - lvgl.widget.hide: loading_screen
          else:
            - script.execute: open_favorites_panel
  platformio_options:
    board_build.flash_mode: dio
    build_flags:
      - "-DCONFIG_LV_DISP_DEF_REFR_PERIOD=10"
      - "-Wno-deprecated-declarations"


# ESP32-C6 coprocessor for WiFi and Bluetooth via SDIO
esp32_hosted:
  variant: esp32c6
  reset_pin: GPIO54
  cmd_pin: GPIO19
  clk_pin: GPIO18
  d0_pin: GPIO14
  d1_pin: GPIO15
  d2_pin: GPIO16
  d3_pin: GPIO17
  active_high: true


psram:
  mode: hex
  speed: 100MHz


# Hide screen during OTA to avoid flicker
ota:
  - platform: esphome
    on_begin:
      - lambda: 'global_preferences->sync();'
      - lvgl.pause
      - light.turn_off:
          id: backlight
          transition_length: 0s
    on_error:
      - light.turn_on:
          id: backlight
          brightness: 100%
          transition_length: 0s
      - lvgl.resume


api:
  on_client_connected:
    - lambda: |-
        if (client_info.find("Home Assistant") != std::string::npos) {
          std::string protocol = "${ha_protocol}";
          std::string host = "${ha_host}";
          // Use the HA API client IP for http (avoids mDNS lookup for artwork),
          // but keep the configured hostname for https (raw IPs won't match SSL certs).
          if (protocol == "http" && host == "homeassistant.local") {
            id(ha_base_url) = "http://" + client_address + ":${ha_port}";
          } else {
            id(ha_base_url) = "${ha_protocol}://${ha_host}:${ha_port}";
          }
        }
    - lvgl.widget.hide: ha_setup_prompt
    - script.execute: apply_timezone
    - lambda: |-
        std::string old_val = id(tv_media_player_entity).state;
        if (!old_val.empty() && id(linked_media_player_entity).state.empty()) {
          ESP_LOGI("media_select", "Migrating TV entity '%s' to linked media player", old_val.c_str());
          auto call = id(linked_media_player_entity).make_call();
          call.set_value(old_val);
          call.perform();
        }
        if (!old_val.empty()) {
          auto call = id(tv_media_player_entity).make_call();
          call.set_value("");
          call.perform();
        }
    - script.execute: subscribe_media_player
    - script.execute: subscribe_linked_media_player
    - script.execute: subscribe_day_night_sensor
  on_client_disconnected:
    - lvgl.widget.hide: setup_prompt
    - if:
        condition:
          lambda: |-
            return !id(device_has_been_setup)
                && !id(boot_grace_period)
                && (esphome::api::global_api_server == nullptr
                    || !esphome::api::global_api_server->is_connected());
        then:
          - lvgl.widget.show: ha_setup_prompt


# -----------------------------------------------------------------------------
# SCREEN ROTATION SELECT
# -----------------------------------------------------------------------------
# Exposes ESPHome 2026.4 native LVGL display rotation to Home Assistant.
# LVGL handles the screen transform and touch input rotation together.
# -----------------------------------------------------------------------------
select:
  - platform: template
    name: "Screen Rotation"
    id: screen_rotation_select
    icon: "mdi:screen-rotation"
    entity_category: config
    internal: ${ha_config_internal}
    optimistic: true
    restore_value: true
    initial_option: "${display_rotation}"
    options:
      - "0"
      - "90"
      - "180"
      - "270"
    on_value:
      - if:
          condition:
            lambda: 'return id(lvgl_ready);'
          then:
            - script.execute: apply_screen_rotation


logger:
  level: DEBUG
  hardware_uart: UART0
  baud_rate: 115200


external_components:
  - source: github://jtenniswood/esphome-media-player@main
    components: [gsl3680]
    refresh: 1s


# DSI PHY power rail
esp_ldo:
  - channel: 3
    id: dsi_phy_enable
    voltage: 2.5V
    adjustable: True


output:
  - platform: ledc
    id: backlight_output
    pin: GPIO23


i2c:
  - id: i2c_bus_a
    sda: GPIO7
    scl: GPIO8
    scan: true
    frequency: 400kHz
    sda_pullup_enabled: False
    scl_pullup_enabled: False


# -----------------------------------------------------------------------------
# GLOBAL STATE VARIABLES
# -----------------------------------------------------------------------------
globals:
  # -- Touch tracking (swipe gesture detection) --
  - id: touch_x_start
    type: int
    initial_value: '0'
  - id: touch_y_start
    type: int
    initial_value: '0'
  - id: touch_x_end
    type: int
    initial_value: '0'
  - id: touch_y_end
    type: int
    initial_value: '0'
  - id: touch_start_time          # millis() when finger first touches (for long-press)
    type: uint32_t
    initial_value: '0'

  # -- UI overlay visibility --
  - id: is_screen_dimmed
    type: bool
    initial_value: 'false'
  - id: was_screen_dimmed
    type: bool
    initial_value: 'false'
  - id: is_ui_hidden
    type: bool
    initial_value: 'false'
  - id: is_clock_showing         # True when clock screensaver overlay is visible
    type: bool
    initial_value: 'false'

  # -- Settings panel state --
  - id: is_panel_open
    type: bool
    initial_value: 'false'
  - id: was_panel_open
    type: bool
    initial_value: 'false'

  # -- Favorites panel state --
  - id: is_favorites_open
    type: bool
    initial_value: 'false'
  - id: was_favorites_open
    type: bool
    initial_value: 'false'

  # -- TV source mode --
  - id: is_tv_mode
    type: bool
    initial_value: 'false'
  - id: is_tv_idle
    type: bool
    initial_value: 'false'

  # -- Favorites content IDs (populated from JSON at boot) --
  - id: fav_content_id_1
    type: std::string
    initial_value: '""'
  - id: fav_content_id_2
    type: std::string
    initial_value: '""'
  - id: fav_content_id_3
    type: std::string
    initial_value: '""'
  - id: fav_content_id_4
    type: std::string
    initial_value: '""'
  - id: fav_content_id_5
    type: std::string
    initial_value: '""'
  - id: fav_content_id_6
    type: std::string
    initial_value: '""'
  - id: fav_content_id_7
    type: std::string
    initial_value: '""'
  - id: fav_content_id_8
    type: std::string
    initial_value: '""'

  # -- Favorites shuffle flags --
  - id: fav_shuffle_1
    type: bool
    initial_value: 'false'
  - id: fav_shuffle_2
    type: bool
    initial_value: 'false'
  - id: fav_shuffle_3
    type: bool
    initial_value: 'false'
  - id: fav_shuffle_4
    type: bool
    initial_value: 'false'
  - id: fav_shuffle_5
    type: bool
    initial_value: 'false'
  - id: fav_shuffle_6
    type: bool
    initial_value: 'false'
  - id: fav_shuffle_7
    type: bool
    initial_value: 'false'
  - id: fav_shuffle_8
    type: bool
    initial_value: 'false'

  # -- Open favorites on next boot (set before player switch reboot) --
  - id: open_favorites_on_boot
    type: bool
    initial_value: 'false'
    restore_value: true

  # -- Pending media player entity during switch --
  - id: pending_media_player
    type: std::string
    restore_value: false

  # -- Unavailable speakers (volume == None in sensor data) --
  - id: speaker_unavailable
    type: bool[16]
    restore_value: false

  # -- Excluded players (from exclude_players.json) --
  - id: excluded_player_count
    type: int
    initial_value: '0'
    restore_value: false
  - id: excluded_player_ids
    type: std::string[16]
    restore_value: false

  # -- Setup flow --
  - id: actions_prompt_acked
    type: bool
    restore_value: true
    initial_value: 'false'
  # True after first successful setup (WiFi + HA + media entity); suppresses
  # WiFi/HA/actions setup prompts on restart so user sees loading/main UI instead.
  - id: device_has_been_setup
    type: bool
    restore_value: true
    initial_value: 'false'
  - id: lvgl_ready
    type: bool
    initial_value: 'false'



# -----------------------------------------------------------------------------
# TOUCHSCREEN (GSL3680)
# -----------------------------------------------------------------------------
touchscreen:
  - platform: gsl3680
    id: tft_touch
    display: tft_display
    reset_pin: GPIO22
    interrupt_pin: GPIO21
    transform:
      mirror_x: true
    on_touch:
      - lambda: |-
          id(was_screen_dimmed) = id(is_screen_dimmed);
          id(was_panel_open) = id(is_panel_open);
          id(was_favorites_open) = id(is_favorites_open);
          id(touch_x_start) = touch.x;
          id(touch_y_start) = touch.y;
          id(touch_x_end) = touch.x;
          id(touch_y_end) = touch.y;
          id(touch_start_time) = millis();
      - script.execute: backlight_wake_timeout
      - lambda: |-
          if (id(is_panel_open)) id(start_speaker_panel_timeout).execute();
    on_update:
      - lambda: |-
          for (auto touch: touches) {
            if (touch.state <= 2) {
              id(touch_x_end) = touch.x;
              id(touch_y_end) = touch.y;
            }
          }
    on_release:
      - lambda: |-
          int dx = id(touch_x_end) - id(touch_x_start);
          int dy = id(touch_y_end) - id(touch_y_start);
          int32_t view_x_start = id(touch_x_start);
          int32_t view_y_start = id(touch_y_start);
          int32_t view_x_end = id(touch_x_end);
          int32_t view_y_end = id(touch_y_end);
          id(main_lvgl).rotate_coordinates(view_x_start, view_y_start);
          id(main_lvgl).rotate_coordinates(view_x_end, view_y_end);
          int view_dx = view_x_end - view_x_start;
          int view_dy = view_y_end - view_y_start;

          ESP_LOGD("touch", "Swipe raw=(%d,%d) view=(%d,%d) rotation=%s start=(%d,%d) end=(%d,%d)",
                   dx, dy, view_dx, view_dy, id(screen_rotation_select).current_option().c_str(),
                   id(touch_x_start), id(touch_y_start),
                   id(touch_x_end), id(touch_y_end));

          // --- Dismiss actions prompt on tap ---
          if (!lv_obj_has_flag(id(actions_prompt), LV_OBJ_FLAG_HIDDEN)) {
            if (abs(view_dx) < 20 && abs(view_dy) < 20) {
              lv_obj_add_flag(id(actions_prompt), LV_OBJ_FLAG_HIDDEN);
              id(actions_prompt_acked) = true;
              id(device_has_been_setup) = true;
              global_preferences->sync();
              App.safe_reboot();
            }
            return;
          }

          // --- Long press (5 s): turn off display ---
          if (abs(view_dx) < 20 && abs(view_dy) < 20 && !id(was_screen_dimmed)
              && (millis() - id(touch_start_time) >= 5000)) {
            id(turn_off_display).execute();
            return;
          }

          // --- Vertical swipe: settings panel open/close ---
          if (view_dy > 20 && abs(view_dy) > abs(view_dx) * 2 && !id(is_panel_open) && !id(is_favorites_open)) {
            ESP_LOGD("touch", "Action: open_settings_panel");
            id(open_settings_panel).execute();
            return;
          }
          if (view_dy < -60 && abs(view_dy) > abs(view_dx) * 2 && id(is_panel_open)
              && (lv_obj_has_flag(id(speakers_container), LV_OBJ_FLAG_HIDDEN) || id(speaker_count) <= 1)) {
            ESP_LOGD("touch", "Action: close_settings_panel");
            id(close_settings_panel).execute();
            return;
          }
          if (view_dy < -60 && abs(view_dy) > abs(view_dx) * 2 && !id(is_panel_open) && !id(is_favorites_open)) {
            ESP_LOGD("touch", "Action: open_favorites_panel");
            id(open_favorites_panel).execute();
            return;
          }
          if (view_dy > 60 && abs(view_dy) > abs(view_dx) * 2 && id(is_favorites_open)) {
            ESP_LOGD("touch", "Action: close_favorites_panel");
            id(close_favorites_panel).execute();
            return;
          }
          if (id(is_panel_open) || id(was_panel_open) || id(is_favorites_open) || id(was_favorites_open)) {
            ESP_LOGD("touch", "Action: ignored_panel_open");
            return;
          }

          // --- Horizontal swipe: track navigation (works when playing or paused) ---
          if (abs(view_dx) > 80 && abs(view_dx) > abs(view_dy) * 2) {
            bool can_skip = false;
            if (id(is_tv_mode)) {
              std::string e = id(linked_media_player_entity).state;
              can_skip = !e.empty() && id(tv_media_player_state_sensor).has_state();
              if (can_skip) {
                std::string s = id(tv_media_player_state_sensor).state;
                can_skip = (s == "playing" || s == "paused");
              }
            } else {
              std::string e = id(media_player_entity).state;
              can_skip = !e.empty() && id(media_player_state_sensor).has_state();
              if (can_skip) {
                std::string s = id(media_player_state_sensor).state;
                can_skip = (s == "playing" || s == "paused");
              }
            }
            if (can_skip) {
              if (view_dx > 0) {
                ESP_LOGD("touch", "Action: previous_track");
                id(swipe_previous_track).execute();
              } else {
                ESP_LOGD("touch", "Action: next_track");
                id(swipe_next_track).execute();
              }
            } else {
              ESP_LOGD("touch", "Action: ignored_track_unavailable");
            }
            return;
          }
          ESP_LOGD("touch", "Action: none");


# -----------------------------------------------------------------------------
# INTERACTION SCRIPTS
# -----------------------------------------------------------------------------
# Track navigation: next/previous (allowed when playing or paused).
script:
  # -- Apply Home Assistant screen-rotation dropdown using native LVGL rotation --
  - id: apply_screen_rotation
    mode: restart
    then:
      - if:
          condition:
            lambda: 'return id(screen_rotation_select).current_option() == "0";'
          then:
            - lvgl.display.set_rotation: 0
      - if:
          condition:
            lambda: 'return id(screen_rotation_select).current_option() == "90";'
          then:
            - lvgl.display.set_rotation: 90
      - if:
          condition:
            lambda: 'return id(screen_rotation_select).current_option() == "180";'
          then:
            - lvgl.display.set_rotation: 180
      - if:
          condition:
            lambda: 'return id(screen_rotation_select).current_option() == "270";'
          then:
            - lvgl.display.set_rotation: 270
      - lambda: |-
          const bool landscape =
            id(screen_rotation_select).current_option() == "90" ||
            id(screen_rotation_select).current_option() == "270";
          const int screen_w = landscape ? 1280 : 800;
          const int screen_h = landscape ? 800 : 1280;

          auto resize_fullscreen_overlay = [](lv_obj_t *obj, lv_coord_t width, lv_coord_t height) {
            if (obj == nullptr) {
              return;
            }
            lv_obj_set_pos(obj, 0, 0);
            lv_obj_set_size(obj, width, height);

            lv_obj_t *content = lv_obj_get_child(obj, 0);
            if (content != nullptr) {
              lv_obj_center(content);
            }
          };

          lv_obj_set_pos(id(artwork_bg_panel), 0, 0);
          lv_obj_set_size(id(artwork_bg_panel), 800, 800);
          lv_obj_set_pos(id(album_art_background_widget), 0, 0);
          lv_obj_set_size(id(album_art_background_widget), 800, 800);
          lv_obj_set_pos(id(artwork_error_label), 200, 380);
          lv_obj_set_width(id(artwork_error_label), 400);

          if (landscape) {
            lv_obj_set_pos(id(accent_overlay), 800, 0);
            lv_obj_set_size(id(accent_overlay), 480, 800);
            lv_obj_set_pos(id(track_info_panel), 840, 40);
            lv_obj_set_size(id(track_info_panel), 400, 720);
            lv_obj_set_style_max_height(id(media_title_label), 506, 0);
          } else {
            lv_obj_set_pos(id(accent_overlay), 0, 800);
            lv_obj_set_size(id(accent_overlay), 800, 480);
            lv_obj_set_pos(id(track_info_panel), 40, 834);
            lv_obj_set_size(id(track_info_panel), 720, 422);
            lv_obj_set_style_max_height(id(media_title_label), 216, 0);
          }

          lv_obj_set_width(id(media_progress_bar), screen_w);
          lv_obj_set_size(id(settings_panel), screen_w, screen_h);
          lv_obj_set_size(id(favorites_panel), screen_w, screen_h);
          lv_obj_set_size(id(clock_screensaver), screen_w, screen_h);

          resize_fullscreen_overlay(id(setup_prompt), screen_w, screen_h);
          resize_fullscreen_overlay(id(actions_prompt), screen_w, screen_h);
          resize_fullscreen_overlay(id(ha_setup_prompt), screen_w, screen_h);
          resize_fullscreen_overlay(id(wifi_setup_prompt), screen_w, screen_h);
          resize_fullscreen_overlay(id(loading_screen), screen_w, screen_h);

          if (!lv_obj_has_flag(id(setup_prompt), LV_OBJ_FLAG_HIDDEN)) {
            lv_obj_move_foreground(id(setup_prompt));
          }
          if (!lv_obj_has_flag(id(actions_prompt), LV_OBJ_FLAG_HIDDEN)) {
            lv_obj_move_foreground(id(actions_prompt));
          }
          if (!lv_obj_has_flag(id(ha_setup_prompt), LV_OBJ_FLAG_HIDDEN)) {
            lv_obj_move_foreground(id(ha_setup_prompt));
          }
          if (!lv_obj_has_flag(id(wifi_setup_prompt), LV_OBJ_FLAG_HIDDEN)) {
            lv_obj_move_foreground(id(wifi_setup_prompt));
          }
          if (!lv_obj_has_flag(id(loading_screen), LV_OBJ_FLAG_HIDDEN)) {
            lv_obj_move_foreground(id(loading_screen));
          }
          lv_obj_update_layout(lv_scr_act());

  - id: swipe_next_track
    mode: single
    then:
      - homeassistant.service:
          service: media_player.media_next_track
          variables:
            target: !lambda |-
              return id(is_tv_mode) ? id(linked_media_player_entity).state : id(media_player_entity).state;
          data_template:
            entity_id: "{{ target }}"
          on_error:
            - lambda: |-
                if (!id(actions_prompt_acked) && !id(device_has_been_setup)) {
                  lv_obj_clear_flag(id(actions_prompt), LV_OBJ_FLAG_HIDDEN);
                }
      - logger.log: "Media: next_track"
  - id: swipe_previous_track
    mode: single
    then:
      - homeassistant.service:
          service: media_player.media_previous_track
          variables:
            target: !lambda |-
              return id(is_tv_mode) ? id(linked_media_player_entity).state : id(media_player_entity).state;
          data_template:
            entity_id: "{{ target }}"
          on_error:
            - lambda: |-
                if (!id(actions_prompt_acked) && !id(device_has_been_setup)) {
                  lv_obj_clear_flag(id(actions_prompt), LV_OBJ_FLAG_HIDDEN);
                }
      - logger.log: "Media: previous_track"

  - id: open_settings_panel
    mode: single
    then:
      - lambda: 'id(is_panel_open) = true;'
      - if:
          condition:
            lambda: 'return id(speaker_count) <= 1;'
          then:
            - script.execute: show_fallback_speaker
      - lvgl.widget.show: settings_panel
      - script.execute: start_speaker_panel_timeout
      - delay: 1000ms
      - script.execute: highlight_active_player
  - id: close_settings_panel
    mode: single
    then:
      - script.stop: start_speaker_panel_timeout
      - lvgl.widget.hide: settings_panel
      - lambda: 'id(is_panel_open) = false;'
  - id: start_speaker_panel_timeout
    mode: restart
    then:
      - if:
          condition:
            lambda: 'return id(speaker_panel_timeout).state > 0;'
          then:
            - delay: !lambda 'return (uint32_t)(id(speaker_panel_timeout).state * 1000);'
            - if:
                condition:
                  lambda: 'return id(is_panel_open);'
                then:
                  - script.execute: close_settings_panel

  - id: toggle_ui
    mode: restart
    then:
      - lambda: 'id(is_ui_hidden) = !id(is_ui_hidden);'

  - id: show_track_view
    mode: single
    then:
      - if:
          condition:
            lambda: 'return id(is_panel_open);'
          then:
            - lvgl.widget.hide: settings_panel
            - lambda: 'id(is_panel_open) = false;'

  - id: enter_tv_mode
    mode: single
    then:
      - lambda: |-
          id(is_tv_mode) = true;
          id(is_tv_idle) = false;
          id(last_tv_artwork_url) = "";
          lv_obj_set_style_bg_color(id(accent_overlay), lv_color_make(0, 0, 0), 0);
          lv_obj_set_style_bg_color(id(artwork_bg_panel), lv_color_make(0, 0, 0), 0);
          lv_obj_set_style_bg_color(id(media_play_pause_button), lv_color_make(0, 0, 0), 0);
      - lvgl.label.update:
          id: media_title_label
          text: !lambda 'return id(media_source_sensor).state;'
      - lambda: 'id(artist_has_content) = false;'
      - lvgl.widget.hide: media_artist_label
      - lvgl.widget.hide: artwork_error_label
      - lambda: 'id(last_tv_progress_percent) = -1;'
      - lvgl.bar.update:
          id: media_progress_bar
          value: 0
          animated: false
      - lvgl.label.update:
          id: media_time_label
          text: "0:00"
      - if:
          condition:
            lambda: 'return id(linked_media_player_entity).state.empty();'
          then:
            - lambda: 'id(is_tv_idle) = true;'
            - lvgl.widget.hide: media_play_pause_button
            - lvgl.widget.hide: media_time_label
            - lvgl.widget.hide: media_progress_bar
            - lvgl.widget.hide: album_art_background_widget
            - script.execute: backlight_wake_timeout
          else:
            - if:
                condition:
                  lambda: |-
                    return id(tv_media_player_state_sensor).has_state()
                        && id(tv_media_player_state_sensor).state == "playing";
                then:
                  - lvgl.widget.hide: media_play_icon
                  - lvgl.widget.show: media_pause_icon
                else:
                  - lvgl.widget.show: media_play_icon
                  - lvgl.widget.hide: media_pause_icon
            - lvgl.image.update:
                id: album_art_background_widget
                src: album_art_placeholder
            - lvgl.widget.update:
                id: album_art_background_widget
                opa: 50%
            - if:
                condition:
                  lambda: |-
                    return id(tv_media_title_sensor).has_state()
                        && !id(tv_media_title_sensor).state.empty();
                then:
                  - lvgl.label.update:
                      id: media_title_label
                      text: !lambda 'return id(tv_media_title_sensor).state;'
            - if:
                condition:
                  lambda: |-
                    return id(tv_media_artist_sensor).has_state()
                        && !id(tv_media_artist_sensor).state.empty()
                        && id(tv_media_artist_sensor).state != "\xe2\x80\x94";
                then:
                  - lambda: 'id(artist_has_content) = true;'
                  - lvgl.label.update:
                      id: media_artist_label
                      text: !lambda 'return id(tv_media_artist_sensor).state;'
                  - lvgl.widget.show: media_artist_label
            - if:
                condition:
                  lambda: |-
                    return id(tv_entity_picture_sensor).has_state()
                        && !id(tv_entity_picture_sensor).state.empty();
                then:
                  - lambda: |-
                      std::string path = id(tv_entity_picture_sensor).state;
                      while (!path.empty() && (path.back() == '\n' || path.back() == '\r' || path.back() == ' ')) path.pop_back();
                      id(last_tv_artwork_url) = (path.substr(0, 7) == "http://" || path.substr(0, 8) == "https://") ? path : (id(ha_base_url) + path);
                      id(last_tv_artwork_url) = esphome::artwork_image::cap_artwork_url(id(last_tv_artwork_url), 800);
                      id(artwork_retry_pending) = false;
                      id(artwork_retry_attempts) = 0;
                  - delay: 50ms
                  - lvgl.image.update:
                      id: album_art_background_widget
                      src: album_art_placeholder
                  - lvgl.widget.update:
                      id: album_art_background_widget
                      opa: 50%
                  - lvgl.widget.hide: album_art_background_widget
                  - delay: 50ms
                  - artwork_image.release: tv_art_background
                  - delay: 100ms
                  - lvgl.widget.show: album_art_background_widget
                  - artwork_image.set_url:
                      id: tv_art_background
                      url: !lambda 'return id(last_tv_artwork_url);'
      - script.execute: show_track_view
      - script.execute: refresh_progress_bar_from_state
      - logger.log: "Entered linked media player mode"

  - id: exit_tv_mode
    mode: single
    then:
      - lambda: |-
          id(is_tv_mode) = false;
          id(is_tv_idle) = false;
          lv_obj_set_style_bg_color(id(artwork_bg_panel), lv_color_make(0, 0, 0), 0);
          lv_obj_set_size(id(album_art_background_widget), ${artwork_display_size}, ${artwork_display_size});
          lv_obj_set_pos(id(album_art_background_widget), 0, 0);
      - lvgl.widget.show: media_play_pause_button
      - lvgl.widget.show: media_time_label
      - lvgl.label.update:
          id: media_title_label
          text: !lambda |-
            return id(media_title_sensor).has_state() && !id(media_title_sensor).state.empty()
                ? id(media_title_sensor).state : std::string("\xe2\x80\x94");
      - if:
          condition:
            lambda: |-
              return id(media_artist_sensor).has_state()
                  && !id(media_artist_sensor).state.empty()
                  && id(media_artist_sensor).state != "\xe2\x80\x94";
          then:
            - lambda: 'id(artist_has_content) = true;'
            - lvgl.label.update:
                id: media_artist_label
                text: !lambda 'return id(media_artist_sensor).state;'
            - lvgl.widget.show: media_artist_label
          else:
            - lambda: 'id(artist_has_content) = false;'
            - lvgl.widget.hide: media_artist_label
      - if:
          condition:
            lambda: |-
              return id(media_player_state_sensor).has_state()
                  && id(media_player_state_sensor).state == "playing";
          then:
            - lvgl.widget.hide: media_play_icon
            - lvgl.widget.show: media_pause_icon
          else:
            - lvgl.widget.show: media_play_icon
            - lvgl.widget.hide: media_pause_icon
      - lvgl.widget.show: album_art_background_widget
      - lvgl.image.update:
          id: album_art_background_widget
          src: album_art_background
      - lvgl.widget.update:
          id: album_art_background_widget
          opa: 100%
      - lvgl.widget.hide: artwork_error_label
      - if:
          condition:
            lambda: |-
              return id(media_entity_picture_sensor).has_state()
                  && !id(media_entity_picture_sensor).state.empty();
          then:
            - lambda: |-
                std::string path = id(media_entity_picture_sensor).state;
                while (!path.empty() && (path.back() == '\n' || path.back() == '\r' || path.back() == ' ')) path.pop_back();
                id(last_artwork_url) = (path.substr(0, 7) == "http://" || path.substr(0, 8) == "https://") ? path : (id(ha_base_url) + path);
                id(last_artwork_url) = esphome::artwork_image::cap_artwork_url(id(last_artwork_url), 800);
                id(artwork_retry_pending) = false;
                id(artwork_retry_attempts) = 0;
            - delay: 50ms
            - lvgl.image.update:
                id: album_art_background_widget
                src: album_art_placeholder
            - lvgl.widget.update:
                id: album_art_background_widget
                opa: 50%
            - lvgl.widget.hide: album_art_background_widget
            - delay: 50ms
            - artwork_image.release: album_art_background
            - delay: 100ms
            - lvgl.widget.show: album_art_background_widget
            - artwork_image.set_url:
                id: album_art_background
                url: !lambda 'return id(last_artwork_url);'
      - script.execute: show_track_view
      - script.execute: refresh_progress_bar_from_state
      - logger.log: "Exited linked media player mode"

  - id: open_favorites_panel
    mode: single
    then:
      - lambda: 'id(is_favorites_open) = true;'
      - lvgl.widget.hide: loading_screen
      - lvgl.widget.show: favorites_panel
      - lambda: |-
          lv_obj_move_foreground(id(favorites_panel));
          lv_obj_set_scroll_dir(id(favorites_grid), LV_DIR_VER);
      - script.execute: start_favorites_panel_timeout

  - id: close_favorites_panel
    mode: single
    then:
      - script.stop: start_favorites_panel_timeout
      - lvgl.widget.hide: favorites_panel
      - lambda: 'id(is_favorites_open) = false;'

  - id: start_favorites_panel_timeout
    mode: restart
    then:
      - if:
          condition:
            lambda: 'return id(speaker_panel_timeout).state > 0;'
          then:
            - delay: !lambda 'return (uint32_t)(id(speaker_panel_timeout).state * 1000);'
            - script.execute: close_favorites_panel

  - id: load_favorites_from_json
    mode: single
    then:
      - lambda: |-
          std::string ha_ip = "";
          if (esphome::api::global_api_server != nullptr) {
            for (auto &client : esphome::api::global_api_server->active_clients()) {
              char buf[esphome::socket::SOCKADDR_STR_LEN];
              const char *peer = client->get_peername_to(std::span<char, esphome::socket::SOCKADDR_STR_LEN>(buf));
              if (peer != nullptr && peer[0] != '\0') {
                ha_ip = std::string(peer);
                auto colon = ha_ip.rfind(':');
                if (colon != std::string::npos) ha_ip = ha_ip.substr(0, colon);
                break;
              }
            }
          }
          if (!ha_ip.empty()) {
            id(ha_base_url) = "http://" + ha_ip + ":8123";
            ESP_LOGI("favorites", "HA discovered at: %s", id(ha_base_url).c_str());
          } else {
            ESP_LOGW("favorites", "No HA API client connected");
          }
      - http_request.get:
          url: !lambda 'return id(ha_base_url) + "/local/favorites.json";'
          capture_response: true
          on_response:
            then:
              - logger.log:
                  format: "favorites.json response: %d"
                  args: [response->status_code]
              - lambda: |-
                  if (response->status_code != 200) {
                    ESP_LOGW("favorites", "Failed to fetch favorites.json: %d", response->status_code);
                    return;
                  }

                  struct Fav { std::string id, name, thumb; bool shuffle = false; };
                  std::vector<Fav> favs;

                  auto find_str = [&](const std::string &src, const std::string &key, size_t from) -> std::pair<std::string, size_t> {
                    size_t k = src.find("\"" + key + "\"", from);
                    if (k == std::string::npos) return {"", std::string::npos};
                    size_t q1 = src.find('"', k + key.size() + 2);
                    if (q1 == std::string::npos) return {"", std::string::npos};
                    q1++;
                    size_t q2 = src.find('"', q1);
                    if (q2 == std::string::npos) return {"", std::string::npos};
                    return {src.substr(q1, q2 - q1), q2};
                  };

                  size_t pos = 0;
                  while (favs.size() < 12) {
                    size_t ob = body.find('{', pos);
                    size_t cb = body.find('}', ob == std::string::npos ? 0 : ob);
                    if (ob == std::string::npos || cb == std::string::npos) break;
                    std::string obj = body.substr(ob, cb - ob + 1);
                    auto [id_v,    p1] = find_str(obj, "id",    0);
                    auto [name_v,  p2] = find_str(obj, "name",  0);
                    auto [thumb_v, p3] = find_str(obj, "thumb", 0);
                    if (!id_v.empty()) {
                      auto [shuffle_v, p4] = find_str(obj, "shuffle", 0);
                      bool shuffle = (shuffle_v == "true");
                      favs.push_back({id_v, name_v, thumb_v, shuffle});
                    }
                    pos = cb + 1;
                  }

                  ESP_LOGI("favorites", "Loaded %d favorites", (int)favs.size());

                  std::string* ids[] = {
                    &id(fav_content_id_1),  &id(fav_content_id_2),  &id(fav_content_id_3),
                    &id(fav_content_id_4),  &id(fav_content_id_5),  &id(fav_content_id_6),
                    &id(fav_content_id_7),  &id(fav_content_id_8),
                  };
                  bool* shuffles[] = {
                    &id(fav_shuffle_1),  &id(fav_shuffle_2),  &id(fav_shuffle_3),
                    &id(fav_shuffle_4),  &id(fav_shuffle_5),  &id(fav_shuffle_6),
                    &id(fav_shuffle_7),  &id(fav_shuffle_8),
                  };
                  lv_obj_t* cards[] = {
                    id(fav_card_1),  id(fav_card_2),  id(fav_card_3),
                    id(fav_card_4),  id(fav_card_5),  id(fav_card_6),
                    id(fav_card_7),  id(fav_card_8),
                  };
                  lv_obj_t* labels[] = {
                    id(fav_label_1),  id(fav_label_2),  id(fav_label_3),
                    id(fav_label_4),  id(fav_label_5),  id(fav_label_6),
                    id(fav_label_7),  id(fav_label_8),
                  };
                  esphome::artwork_image::ArtworkImage* arts[] = {
                    id(fav_art_1),  id(fav_art_2),  id(fav_art_3),
                    id(fav_art_4),  id(fav_art_5),  id(fav_art_6),
                    id(fav_art_7),  id(fav_art_8),
                  };

                  for (int i = 0; i < 8; i++) {
                    if (i < (int)favs.size()) {
                      *ids[i] = favs[i].id;
                      *shuffles[i] = favs[i].shuffle;
                      lv_label_set_text(labels[i], favs[i].name.empty() ? " " : favs[i].name.c_str());
                      if (favs[i].name.empty()) {
                        lv_obj_set_style_bg_opa(labels[i], LV_OPA_TRANSP, 0);
                      } else {
                        lv_obj_set_style_bg_opa(labels[i], LV_OPA_30, 0);
                      }
                      if (!favs[i].thumb.empty()) {
                        std::string thumb_url = favs[i].thumb;
                        if (!thumb_url.empty() && thumb_url[0] == '/') {
                          thumb_url = id(ha_base_url) + thumb_url;
                        }
                        arts[i]->request_update_url(thumb_url);
                      }
                      lv_obj_clear_flag(cards[i], LV_OBJ_FLAG_HIDDEN);
                    } else {
                      *ids[i] = "";
                      lv_obj_add_flag(cards[i], LV_OBJ_FLAG_HIDDEN);
                    }
                  }

  - id: fav_play_1
    mode: single
    then:
      - if:
          condition:
            lambda: 'return id(fav_shuffle_1);'
          then:
            - homeassistant.service:
                service: media_player.shuffle_set
                data_template:
                  entity_id: "{{ target }}"
                  shuffle: "true"
                variables:
                  target: !lambda 'return id(media_player_entity).state;'
      - homeassistant.service:
          service: media_player.play_media
          variables:
            target: !lambda 'return id(media_player_entity).state;'
            content: !lambda 'return id(fav_content_id_1);'
          data_template:
            entity_id: "{{ target }}"
            media_content_id: "{{ content }}"
            media_content_type: "favorite_item_id"
      - script.execute: close_favorites_panel

  - id: fav_play_2
    mode: single
    then:
      - if:
          condition:
            lambda: 'return id(fav_shuffle_2);'
          then:
            - homeassistant.service:
                service: media_player.shuffle_set
                data_template:
                  entity_id: "{{ target }}"
                  shuffle: "true"
                variables:
                  target: !lambda 'return id(media_player_entity).state;'
      - homeassistant.service:
          service: media_player.play_media
          variables:
            target: !lambda 'return id(media_player_entity).state;'
            content: !lambda 'return id(fav_content_id_2);'
          data_template:
            entity_id: "{{ target }}"
            media_content_id: "{{ content }}"
            media_content_type: "favorite_item_id"
      - script.execute: close_favorites_panel

  - id: fav_play_3
    mode: single
    then:
      - if:
          condition:
            lambda: 'return id(fav_shuffle_3);'
          then:
            - homeassistant.service:
                service: media_player.shuffle_set
                data_template:
                  entity_id: "{{ target }}"
                  shuffle: "true"
                variables:
                  target: !lambda 'return id(media_player_entity).state;'
      - homeassistant.service:
          service: media_player.play_media
          variables:
            target: !lambda 'return id(media_player_entity).state;'
            content: !lambda 'return id(fav_content_id_3);'
          data_template:
            entity_id: "{{ target }}"
            media_content_id: "{{ content }}"
            media_content_type: "favorite_item_id"
      - script.execute: close_favorites_panel

  - id: fav_play_4
    mode: single
    then:
      - if:
          condition:
            lambda: 'return id(fav_shuffle_4);'
          then:
            - homeassistant.service:
                service: media_player.shuffle_set
                data_template:
                  entity_id: "{{ target }}"
                  shuffle: "true"
                variables:
                  target: !lambda 'return id(media_player_entity).state;'
      - homeassistant.service:
          service: media_player.play_media
          variables:
            target: !lambda 'return id(media_player_entity).state;'
            content: !lambda 'return id(fav_content_id_4);'
          data_template:
            entity_id: "{{ target }}"
            media_content_id: "{{ content }}"
            media_content_type: "favorite_item_id"
      - script.execute: close_favorites_panel

  - id: fav_play_5
    mode: single
    then:
      - if:
          condition:
            lambda: 'return id(fav_shuffle_5);'
          then:
            - homeassistant.service:
                service: media_player.shuffle_set
                data_template:
                  entity_id: "{{ target }}"
                  shuffle: "true"
                variables:
                  target: !lambda 'return id(media_player_entity).state;'
      - homeassistant.service:
          service: media_player.play_media
          variables:
            target: !lambda 'return id(media_player_entity).state;'
            content: !lambda 'return id(fav_content_id_5);'
          data_template:
            entity_id: "{{ target }}"
            media_content_id: "{{ content }}"
            media_content_type: "favorite_item_id"
      - script.execute: close_favorites_panel

  - id: fav_play_6
    mode: single
    then:
      - if:
          condition:
            lambda: 'return id(fav_shuffle_6);'
          then:
            - homeassistant.service:
                service: media_player.shuffle_set
                data_template:
                  entity_id: "{{ target }}"
                  shuffle: "true"
                variables:
                  target: !lambda 'return id(media_player_entity).state;'
      - homeassistant.service:
          service: media_player.play_media
          variables:
            target: !lambda 'return id(media_player_entity).state;'
            content: !lambda 'return id(fav_content_id_6);'
          data_template:
            entity_id: "{{ target }}"
            media_content_id: "{{ content }}"
            media_content_type: "favorite_item_id"
      - script.execute: close_favorites_panel

  - id: fav_play_7
    mode: single
    then:
      - if:
          condition:
            lambda: 'return id(fav_shuffle_7);'
          then:
            - homeassistant.service:
                service: media_player.shuffle_set
                data_template:
                  entity_id: "{{ target }}"
                  shuffle: "true"
                variables:
                  target: !lambda 'return id(media_player_entity).state;'
      - homeassistant.service:
          service: media_player.play_media
          variables:
            target: !lambda 'return id(media_player_entity).state;'
            content: !lambda 'return id(fav_content_id_7);'
          data_template:
            entity_id: "{{ target }}"
            media_content_id: "{{ content }}"
            media_content_type: "favorite_item_id"
      - script.execute: close_favorites_panel

  - id: fav_play_8
    mode: single
    then:
      - if:
          condition:
            lambda: 'return id(fav_shuffle_8);'
          then:
            - homeassistant.service:
                service: media_player.shuffle_set
                data_template:
                  entity_id: "{{ target }}"
                  shuffle: "true"
                variables:
                  target: !lambda 'return id(media_player_entity).state;'
      - homeassistant.service:
          service: media_player.play_media
          variables:
            target: !lambda 'return id(media_player_entity).state;'
            content: !lambda 'return id(fav_content_id_8);'
          data_template:
            entity_id: "{{ target }}"
            media_content_id: "{{ content }}"
            media_content_type: "favorite_item_id"
      - script.execute: close_favorites_panel




# -----------------------------------------------------------------------------
# DISPLAY (MIPI DSI - JC8012P4A1)
# -----------------------------------------------------------------------------
  - id: load_exclude_players
    mode: single
    then:
      - http_request.get:
          url: !lambda 'return id(ha_base_url) + "/local/exclude_players.json";'
          capture_response: true
          on_response:
            then:
              - lambda: |-
                  id(excluded_player_count) = 0;
                  if (response->status_code != 200) {
                    ESP_LOGI("exclude", "No exclude_players.json found (status %d) — no exclusions", response->status_code);
                    return;
                  }
                  size_t pos = 0;
                  int count = 0;
                  while (count < 16) {
                    size_t q1 = body.find('"', pos);
                    if (q1 == std::string::npos) break;
                    q1++;
                    size_t q2 = body.find('"', q1);
                    if (q2 == std::string::npos) break;
                    std::string entity = body.substr(q1, q2 - q1);
                    if (!entity.empty() && entity.substr(0, 13) == "media_player.") {
                      id(excluded_player_ids)[count++] = entity;
                      ESP_LOGI("exclude", "Excluding player: %s", entity.c_str());
                    }
                    pos = q2 + 1;
                  }
                  id(excluded_player_count) = count;
                  ESP_LOGI("exclude", "Loaded %d excluded players", count);


  - id: parse_unavailable_speakers
    mode: single
    then:
      - lambda: |-
          for (int i = 0; i < 16; i++) id(speaker_unavailable)[i] = false;
          std::string raw = id(speakers_sensor).state;
          if (raw.empty() || raw == "None" || raw == "unknown") return;
          size_t p1 = raw.find('|');
          if (p1 == std::string::npos) return;
          size_t p2 = raw.find('|', p1 + 1);
          if (p2 == std::string::npos) return;
          size_t p3 = raw.find('|', p2 + 1);
          std::string vol_str = raw.substr(p2 + 1, p3 == std::string::npos ? std::string::npos : p3 - p2 - 1);
          int idx = 0;
          size_t pos = 0;
          while (idx < 16) {
            size_t comma = vol_str.find(',', pos);
            std::string vol = vol_str.substr(pos, comma == std::string::npos ? std::string::npos : comma - pos);
            if (vol == "None" || vol == "unknown" || vol.empty()) {
              id(speaker_unavailable)[idx] = true;
              ESP_LOGI("exclude", "Speaker %d unavailable (vol=%s)", idx, vol.c_str());
            }
            idx++;
            if (comma == std::string::npos) break;
            pos = comma + 1;
          }



  - id: highlight_active_player
    mode: restart
    then:
      - script.execute: parse_unavailable_speakers
      - lambda: |-
          std::string active = id(media_player_entity).state;
          lv_obj_t *cards[] = {
            id(speaker_card_0), id(speaker_card_1), id(speaker_card_2), id(speaker_card_3),
            id(speaker_card_4), id(speaker_card_5), id(speaker_card_6), id(speaker_card_7),
            id(speaker_card_8), id(speaker_card_9), id(speaker_card_10), id(speaker_card_11),
            id(speaker_card_12), id(speaker_card_13), id(speaker_card_14), id(speaker_card_15)
          };
          int excl_count = id(excluded_player_count);
          for (int i = 0; i < id(speaker_count); i++) {
            // Hide excluded players regardless of grouping state
            bool excluded = false;
            for (int e = 0; e < excl_count; e++) {
              if (id(speaker_entity_ids)[i] == id(excluded_player_ids)[e]) {
                excluded = true;
                break;
              }
            }
            if (excluded) {
              lv_obj_add_flag(cards[i], LV_OBJ_FLAG_HIDDEN);
              continue;
            }
            // Hide unavailable speakers
            if (id(speaker_unavailable)[i]) {
              lv_obj_add_flag(cards[i], LV_OBJ_FLAG_HIDDEN);
              continue;
            }
            // Only highlight active player — don't touch grouped card styling
            // update_grouping_panel already set card colors for group state
            if (!lv_obj_has_flag(cards[i], LV_OBJ_FLAG_HIDDEN) &&
                id(speaker_entity_ids)[i] == active) {
              lv_obj_set_style_border_color(cards[i], lv_color_white(), 0);
              lv_obj_set_style_border_width(cards[i], 3, 0);
              lv_obj_set_style_border_opa(cards[i], LV_OPA_COVER, 0);
            }
          }


  - id: switch_media_player
    mode: single
    parameters:
      slot: int
    then:
      - lambda: |-
          if (slot < 0 || slot >= id(speaker_count)) return;
          std::string entity = id(speaker_entity_ids)[slot];
          if (entity == id(media_player_entity).state) return;
          id(open_favorites_on_boot) = true;
          ESP_LOGI("media_select", "Switching media player to: %s", entity.c_str());
          id(pending_media_player) = entity;
          lv_label_set_text(id(loading_status_label), "Rebooting");
          lv_obj_clear_flag(id(loading_screen), LV_OBJ_FLAG_HIDDEN);
          lv_obj_move_foreground(id(loading_screen));
      - homeassistant.service:
          service: media_player.media_stop
          data_template:
            entity_id: !lambda 'return id(media_player_entity).state;'
      - delay: 500ms
      - lambda: |-
          if (id(pending_media_player).empty()) return;
          auto call = id(media_player_entity).make_call();
          call.set_value(id(pending_media_player));
          call.perform();
      - script.execute: highlight_active_player


display:
  - platform: mipi_dsi
    model: JC8012P4A1
    id: tft_display
    reset_pin: GPIO27
    auto_clear_enabled: false
    color_order: RGB
    dimensions:
      width: 800
      height: 1280
